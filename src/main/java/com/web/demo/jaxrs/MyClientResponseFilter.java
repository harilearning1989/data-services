package com.web.demo.jaxrs;

/*@Provider
public class MyClientResponseFilter implements ClientResponseFilter {
    @Override
    public void filter(ClientRequestContext requestContext,
                       ClientResponseContext responseContext) throws IOException {
        System.out.println("Client received response with status: " + responseContext.getStatus());

        // Log response headers
        responseContext.getHeaders().forEach((key, value) -> {
            System.out.println(key + ": " + value);
        });
    }
}*/

